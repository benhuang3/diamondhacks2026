# Copy to .env.local and adjust.
# Backend API base URL (no trailing slash)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
# Set to "true" to show seeded demo data (no backend needed)
NEXT_PUBLIC_DEMO_MODE=true
